{
    "tasks": {
        "test": "deno test -A --watch src",
        "build": "clear && deno run -A scripts/build_npm.ts",
        "publish": "clear && cd npm && npm publish"
    },
    "importMap": "./import_map.json",
    "compilerOptions": {
        "noImplicitAny": false,
        "suppressImplicitAnyIndexErrors": true
    }
}